name: keepalive

on:
  schedule:
    - cron: "*/5 * * * *"   # 5分おき
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render site
        run: |
          curl -I -L --max-time 20 https://cigar-importprice.onrender.com/ || true
